# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=feedsystemdb

# Opcional: Logging de queries SQL (true/false)
DB_ECHO=false

# Autenticación JWT
JWT_SECRET=onix-demo-secret-key-2026-feeding-system
JWT_EXPIRATION_HOURS=24

# CORS - origins permitidos (comma-separated). Si no se define, permite todos (*)
CORS_ORIGINS=http://localhost:3000